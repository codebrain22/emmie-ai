enum ValueType {
  string,
  integer,
  boolean,
  double,
  stringList,
}

enum ThemeModeOptions {
  light,
  dark,
}

enum AppUserActions {
  speech,
  clear,
  disable,
  delete,
  deleteAll,
}

enum SettingsOptions {
  themeMode,
  voiceAssistance,
  notificationsMute,
  notificationsPause,
  language,
}

enum SettingsItems {
  notifications,
  appearance,
  language,
  share,
  rateApp,
  signOut,
}

enum Entitlement {
  free,
  premium,
}
